#include "sort.h"


listint *gotoplc(listint *node)
{
	traverse;
}

void swaploc(listint *node1, listint *node2)
{
	swap;
}

void insertion_sort_list(listint_t **list)
{
	listint_t *one = *list;
	listint_t *two = NULL;
	/* if no list or one element */
	if (!list || !one || !one->next)
		return;
	/*  */
	while (one)
	{
		if (one->n > two->n)
		{
			re-traverse func;
			
			swap func;
	
			print;
			traverse;
		}
		else
		{
			traverse
			advance one
		}
	}
}	
